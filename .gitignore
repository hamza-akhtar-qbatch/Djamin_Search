# secrets - never commit
.env
web/.env.local

# dependencies & build output
node_modules/
.next/
*.tsbuildinfo

# downloaded audio for the raw enrichment cache — regenerable via enrich.py,
# not needed at runtime (web/public/test_audio/ IS committed: those two
# reference-track MP3s are served by the deployed app)
audio_cache/

# logs & transient
*.log
search_log.jsonl
web/search_log.jsonl
djaminn_demo_portable.zip
.DS_Store
